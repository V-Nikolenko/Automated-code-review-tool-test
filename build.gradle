allprojects {
    apply plugin: 'java'
    apply plugin: 'idea'
    apply plugin: 'checkstyle'

    group = 'org.geekhub'

    repositories {
        mavenCentral()
    }

    idea {
        module {
            sourceDirs += file('src/main/java')
        }
    }
}